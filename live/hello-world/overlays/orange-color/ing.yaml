apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-world-ing
spec:
  rules:
    - host: ams.mak3r.design
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service: 
              name: hello-world-svclb
              port:
                number: 8080 